import * as React from 'react';

// import { getDataRunStepsInfo } from '../../service/dataService';

export interface LeaderBoardProps {
    datarunID: number;
}

export default class LeaderBoard extends React.Component<LeaderBoardProps, any> {
  public render() {
    return (
      <div/>
    );
  }
}
